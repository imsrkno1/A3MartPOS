# instance/.env
# Generate a strong secret key (e.g., using python -c 'import secrets; print(secrets.token_hex(16))')
SECRET_KEY='YOUR_VERY_STRONG_RANDOM_SECRET_KEY_HERE'

# Database URL (Example for SQLite in the instance folder)
DATABASE_URL='sqlite:///a3mart.db'

# Example for MySQL (replace with your actual credentials if you switch later)
# DATABASE_URL='mysql+mysqlclient://username:password@host/database_name'

# Example for PostgreSQL
# DATABASE_URL='postgresql+psycopg2://username:password@host/database_name'